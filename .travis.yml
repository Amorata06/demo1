arch:
  - ppc64le
os: linux
services:
  - docker
  - build:./Dockerfiles
language: python
python:
  - "3.6"
before_install:
  - docker pull alpine 
script: 
  - docker build -t hello-py Dockerfiles/
  - docker images
  - docker run hello-py
